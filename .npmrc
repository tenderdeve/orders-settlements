# pnpm's default symlinked node_modules is deliberate: a package imported but not
# declared fails at install rather than in production. Do not add
# shamefully-hoist=true. If Next's standalone build ever throws MODULE_NOT_FOUND
# in the container, the narrow fix is `node-linker=hoisted` on this line only.
strict-peer-dependencies=false
